"""Tests Summarizer output text length. """

"""
from app.summarizer import Summarizer





def test_summarizer_summarizes():
    # use default arguments
    summarizer = Summarizer(max_length=10)
  

    text =  "A wonderful serenity has taken possession of my entire soul."
    summary = summarizer.summarize_text(text=text)

    assert len(summary) == 10
    assert type(summary) == str

    summarizer = Summarizer(max_length=10)
    summary = summarizer.summarize_text(text=text)

    assert len(summary) == 5
    assert type(summary) == str
    """