services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  job-service:
    build: ./job-service
    ports:
      - "8000:8000"
    environment:
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      QUEUE_NAME: jobs
    depends_on:
      rabbitmq:
        condition: service_healthy

  notification-service:
    build: ./notification-service
    ports:
      - "8001:8001"

  model-service:
    build: ./model-service
    environment:
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      QUEUE_NAME: jobs
      NOTIFICATION_URL: http://notification-service:8001/api/notification
    depends_on:
      rabbitmq:
        condition: service_healthy
      notification-service:
        condition: service_started
    volumes: 
      - model-service-weights:/home/appuser/.cache/huggingface

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - job-service
      - notification-service

volumes:
  model-service-weights: